# OpenClaw Email Assistant environment variables

# OpenClaw gateway API key (alternatively put in config)
OPENCLAW_API_KEY=your-openclaw-api-key-here

# Gmail OAuth credentials file location (optional, overrides config)
# GMAIL_CREDENTIALS_JSON=/path/to/credentials.json
